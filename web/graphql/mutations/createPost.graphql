mutation CreatePost($name: String!, $wysiwygText: String!, $slug: String!) {
  createPost(name: $name, wysiwygText: $wysiwygText, slug: $slug) {
    id
    name
    published
    wysiwygText
    slug
  }
}
